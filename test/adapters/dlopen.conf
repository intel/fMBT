# quick test howto:
# src$ g++ -fPIC -shared -o dlopen_dummy.so adapter_dummy.cc adapter.cc
# src$ LD_LIBRARY_PATH=$(pwd)

model = "abc.lsts"
heuristic = "greedy"
adapter = "dlopen:dlopen_dummy.so,dummy"
coverage = "perm:1"
engine.cov   = 1.0
engine.count = -1
