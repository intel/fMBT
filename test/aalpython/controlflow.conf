model     = aal_remote(remote_pyaal -l controlflow.aal.log -DOUTPUTS 'controlflow.aal')
adapter   = aal
heuristic = lookahead(6)
coverage  = perm(1)
fail      = steps(9)
on_pass   = exit(0)
on_fail   = exit(1)
on_inconc = exit(2)