model     = aal_remote(remote_pyaal -l "times.aal.log" "times.aal")
adapter   = aal
heuristic = lookahead(4)
coverage  = perm(3)

learn     = time(0.1)

pass      = steps(40)
on_pass   = exit(0)
on_fail   = exit(1)
on_inconc = exit(2)