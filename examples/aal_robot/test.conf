model     = aal_remote(remote_pyaal -l "robot.aal.log" "robot.aal")
adapter   = aal
heuristic = lookahead(4)
coverage  = perm(2)
pass      = steps(10)
on_pass   = exit(0)
on_fail   = exit(1)
on_inconc = exit(2)